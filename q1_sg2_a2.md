#13 #14 #15

1. Pseudocode 1 is faster if the list of numbers is large because it  has only one nested loop causing the program to end with fewer actions. It also does not repeat work unnecessarily unlike Pseudocode 2 which compares each number to each other even if that number was already shown to be less than another number in previous comparisons, leading to potentially redundant and repeated steps.

2. Pseudocode 1 is easier to understand because 'max' as a variable name is more accurate than 'bigger' as we are looking for the number with the highest value, and since it has one nested loop instead of 2 it is simpler to read and has fewer lines of code.

3. I would have an easier time modifying pseudocode one. This is due to the fact that the second pseudocode has two loops, which makes it more tedious to construct new code for the min.

4. Pseudocode 1, since it is faster and more efficient in calculating the largest number compared to pseudocode 2. This makes testing multiple numbers easier for the user.

5. Both Pseudocodes have to first, validate that the list is not empty/null to ensure that it is safe from index-out-of-bounds crashes, as well as checking if the list contains any non-numerical values to produce clear output. Pseudocode 2 also needs a limit on the size of the list to prevent program freezes due to its loops and large number of steps. 

6. Based on our answers from 1 to 5, the better algorithm is the first pseudocode. It is easier to understand, faster and more efficient to use, and overall better for clarification and upgrading. Pseudocode 2 is noticeably harder and less efficient than the first, since it goes through tons of unnecessary steps.
